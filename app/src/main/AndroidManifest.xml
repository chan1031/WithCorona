<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBQz_VnAPm7J6nXr8m7NDKvNO5osiQhw2w" />
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
        <activity android:name=".login.Loding">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Main2"/>
        <activity android:name=".login.LoginPage"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".join.Terms"/>
        <activity android:name=".join.Join"/>
        <activity android:name=".join.LoginSuccess"/>
        <activity android:name=".Gps.SubActivity"
            android:launchMode="singleTask"/>
    </application>

</manifest>